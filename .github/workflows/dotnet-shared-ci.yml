name: .NET Shared CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      reason: 
        description: "Why do you want to run this workflow?"
        required: true        

jobs:
  build:
    uses: SolidifyDemo/actions-training-221205-shared/.github/workflows/shared-dotnet-ci.yml@f162a1f6a88c254c33c4c0782cd38524a33ab1ab
